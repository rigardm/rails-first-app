<div class="container">
  <%= simple_form_for[@hotel, @room] do |f| %>
    <%= f.input :number %>
    <%= f.input :capacity %>
    <%= f.input :daily_price, as: :integer %>
    <%= f.submit %>
  <% end %>
</div>
